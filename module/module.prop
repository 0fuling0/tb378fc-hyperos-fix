id=tb378fc_hyperos_fix_lite
name=TB378FC HyperOS 修复 Lite
version=v1.0
versionCode=10000
author=ACLaniakea, fuling
description=联想小新 Pad Pro GT 13 (TB378FC) 刷 HyperOS 移植包的【纯系统修复】版：只修移植包自身坏掉、靠 root 就能修的部分。**不含任何常驻守护进程，不安装任何 APK，不需要 LSPosed。** ②PowerKeeper——修补移植包改坏的两处字节码（void 方法里 return 值 / 静态方法丢了 static），恢复 MIUI 省电管理；③停 BPF 监视器——开机后一次性停掉 hyper_bpfloader 的监视器（dynbpfloader），避免它判定"系统损坏"去写 recovery 引导块重启进 recovery；④停死电话栈——本机 ro.baseband=apq 无 modem，停掉每秒崩数百次的三个包；⑭开发者选项——补 system_app 读写 logpersistd_logging_prop 两条最小权限，否则 Enforcing 下打开「设置→开发者选项」必现闪退。**全部动作在开机后执行一次即退出，没有任何常驻进程。** 开关可在 KernelSU 管理器的 WebUI 里改，也可用模块目录下的 disable-* 标记文件强制关。需要 App 的手写笔功能（唤醒/电量胶囊/手势桥/笔刷触感/注视感知等）见同仓库 Full 分支。
