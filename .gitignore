# 构建产物
out/

# payload 中间产物（payload-src 下的原厂 APK 不入库 —— 体积大且版权在原厂；
# 补丁脚本 patch_powerkeeper.py / fix_static.py / repack_payload.py 入库）
payload-src/PowerKeeper-patched.apk
payload-src/classes-patched*.dex
payload-src/payload.apk
payload-src/PowerKeeper-stock.apk

# 设备/调试残留 —— 模块目录下这些是**运行期**文件，既不能进仓库也不能进包
*.log
*.pid
brush.*
wake.log*
__pycache__/
*.pyc
module/disable
module/disable-*
